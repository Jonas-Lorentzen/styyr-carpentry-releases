# STYYR – utgivelser for laptopen

Oppdateringsfeeden for STYYR-desktop (snekker). Hver utgivelse (`v<versjon>`) bærer installereren, blockmap-en og `latest.yml`; installerte kopier leser feeden selv. Publiseres med `npm run release -w desktop`.
